# Hello World Program

This project provides a minimal, single-file Python program that prints "Hello World" to standard output and exits with a success code.

## How to Run

Execute the script using a Python 3 interpreter:

```sh
python3 hello.py
```

## Expected Output

The command will produce the following output on standard output, followed by a newline:

```
Hello World
```
